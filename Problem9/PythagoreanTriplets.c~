#include <stdio.h>

int main(){

  int a=0,b=0,c=0,m=0,n=0;

  while(a+b+c != 1000 || a+b+c > 1000){
    a=m*m-n*n;
    b=2*m*n;
    c=m*m-n*n;
    m++;
    n++;
  }

  printf("a = %d\n", a);
  printf("b = %d\n", b);
  printf("c = %d\n", c);

}
